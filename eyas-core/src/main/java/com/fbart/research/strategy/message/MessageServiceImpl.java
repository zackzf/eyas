package com.fbart.research.strategy.message;
public class MessageServiceImpl implements MessageService {

    @Override
    public void send(MessageParam messageParam) {
        if (messageParam.getMessageType().equals(1)) {

        } else if (messageParam.getMessageType().equals(2)) {

        }
    }
}
